// Checks if the game state is paused
if (curr_game_state == GAME_STATE.PAUSED)
{
	// Changes previous state to true
	was_paused = true;
}
else
{
	// Changes previous state to false
	was_paused = false;	
}