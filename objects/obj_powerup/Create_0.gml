// Powerup pickup that spawns when zombies die.
powerup_type = "health";  // overridden by spawner
lifetime = 600;            // 10 seconds before disappearing
bob_phase = random(360);   // random bob offset for visual variety
